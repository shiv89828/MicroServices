#below name should be same as file in git repo and 
spring.application.name=limit-service
#limit-service.minimum=1
#limit-service.maximum=9999

#to retreive configs from other mcroservice
spring.cloud.config.uri=http://localhost:8888
#which profile to use for different env. ..if nothing default will be picked
spring.profiles.active=dev
